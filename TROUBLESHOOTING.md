# 问题排除指南 (Troubleshooting Guide)

> 记录项目开发过程中遇到的典型问题及解决方案，供将来参考。

---

## 问题 #1：代码丢失 - 找不到最新开发的功能

### 📅 发生时间
2025-11-28

### 🔍 问题描述
开发了"VIBE CODING + 绿点状态灯"版本的详情页面（包含视图管理、无刷新切换等功能），但在 GitHub 仓库和当前工作目录中都找不到这些代码。

### 🎯 排查过程

#### 1. 检查 Git 提交历史
```bash
git log --oneline -20
git log --pretty=format:'%h %ad %s' --date=short
```
**结果**：历史记录中没有相关提交

#### 2. 检查 Git Reflog（找回丢失的提交）
```bash
git reflog -20
```
**结果**：发现有一些 reset 操作，但没有找到目标代码

#### 3. 检查隐藏的 Stash
```bash
git stash list
```
**结果**：没有 stash

#### 4. 检查所有分支
```bash
git branch -a
```
**结果**：发现远程分支 `origin/fix/home-complete-single-click`，但不是目标代码

#### 5. 🎯 关键步骤：搜索其他工作目录
```bash
find /Users/apple -name "zhuyili" -type d 2>/dev/null
```
**结果**：发现多个 zhuyili 目录！
- `/Users/apple/注意力跳动问题修复/zhuyili` (当前目录)
- `/Users/apple/zhuyili` ← **找到了！**

#### 6. 检查另一个目录的状态
```bash
cd /Users/apple/zhuyili
git status
```
**结果**：发现大量未提交的修改！
```
modified:   combined.js
modified:   index.html
modified:   multi-stopwatch-fixed.js
```

#### 7. 查看修改内容确认
```bash
git diff --stat
```
**结果**：+1429 行修改，正是丢失的代码！

### ✅ 解决方案

1. **复制修改到当前工作目录**
```bash
cp /Users/apple/zhuyili/index.html \
   /Users/apple/zhuyili/combined.js \
   /Users/apple/zhuyili/multi-stopwatch-fixed.js \
   /Users/apple/注意力跳动问题修复/zhuyili/
```

2. **提交并推送**
```bash
git add -A
git commit -m "✨ 恢复丢失的代码：视图管理、计时器详情页、无刷新切换功能"
git push origin main
```

### 📝 经验教训

1. **多目录问题**：同一项目可能存在多个工作目录，修改可能在不同目录中
2. **未提交代码**：重要修改要及时提交，避免丢失
3. **排查顺序**：
   - Git 历史 → Reflog → Stash → 分支 → **其他目录**
4. **find 命令**：是找回代码的关键工具

### 🔧 预防措施

1. 统一使用一个工作目录
2. 开发完成后及时 `git commit` 和 `git push`
3. 使用 VS Code 的 Source Control 面板查看未提交修改
4. 定期清理不用的项目副本

---

## 问题 #2：（预留）

### 📅 发生时间
（待填写）

### 🔍 问题描述
（待填写）

### 🎯 排查过程
（待填写）

### ✅ 解决方案
（待填写）

---

## 常用排查命令速查

```bash
# Git 历史
git log --oneline -20
git log --pretty=format:'%h %ad %s' --date=short

# Git Reflog（找回丢失的提交）
git reflog -20

# 查看 Stash
git stash list

# 查看所有分支
git branch -a

# 查看远程分支内容
git log --oneline origin/branch-name -10

# 查找项目目录
find /Users/apple -name "项目名" -type d 2>/dev/null

# 查看未提交修改
git status
git diff --stat

# 搜索代码内容
grep -r "关键词" /path/to/search
```

---

*最后更新：2025-11-28*
